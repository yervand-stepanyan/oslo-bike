import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  logoImage: {
    height: '35px',
    width: '50px',
  },
  titleWrapper: {
    marginLeft: '12px',
  },
});
